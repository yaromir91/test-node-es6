import Joi from 'joi';

export default {
  // POST /api/products
  createProduct: {
    body: {
    }
  },

  // UPDATE /api/products/:userId
  updateProduct: {
    body: {
    },
    params: {
    }
  }
};
